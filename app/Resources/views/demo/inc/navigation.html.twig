{% for link in navigation %}
    <a class="mdl-navigation__link" href="{{ link.href }}">
        <i class="mdl-color-text--blue-grey-400 material-icons" role="presentation">{{ link.icon }}</i>
        <span>{{ link.title }}</span>
    </a>
{% endfor %}